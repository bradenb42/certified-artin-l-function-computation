"""A standalone checker for the certificates.

This package shares only the permutation group module with the main package.
All of its other arithmetic is its own, so a bug in one implementation is
unlikely to be reproduced by the other.
"""
